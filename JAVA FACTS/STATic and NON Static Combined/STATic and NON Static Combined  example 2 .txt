In Java, if you define a **static** and a **non-static** (instance) variable with the **same name**, the static one belongs to the class and the non-static one belongs to the object. You can **access the static variable directly using the class name**, and the **non-static variable using an object**. If you're asking how to **print the correct one without using reference (i.e., no object reference)**, here's how you can do it:

#